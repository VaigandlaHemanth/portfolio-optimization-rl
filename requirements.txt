stable-baselines3==2.1.0
gym==0.25.2
shimmy==1.1.0
torch==2.1.0
numpy==1.24.4
pandas==2.0.3
matplotlib==3.7.2
yfinance==0.2.28

# dev / tests (offline — metrics, regimes, baselines need only numpy + pytest)
pytest>=7.0
